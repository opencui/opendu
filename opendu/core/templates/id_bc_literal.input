# Intent Detection: "{{skill.name}}"

## Task
Analyze the given utterance and determine if it matches the intent "{{skill.name}}": {{skill.description}}

Output only "True" or "False".

## Decision Guidelines
- **True**: The utterance clearly expresses the intent described above
- **False**: The utterance does not match this intent or expresses a different intent

{% if examples %}
## Examples
{% for example in examples %}
**Utterance**: "{{example.template}}"
**Decision**: {{example.label}}

{% endfor %}
{% endif %}

## Analysis Framework
1. **Intent Match**: Does the utterance align with "{{skill.description}}"?
2. **Context Check**: Consider the full context and meaning
3. **Precision**: Avoid false positives - be specific about intent matching

---

{% if arguments %}
**Context Arguments**:
{% for key, value in arguments.items() %}
- {{key}}: {{value}}
{% endfor %}

{% endif %}
**Utterance**: "{{utterance}}"

**Decision**: 